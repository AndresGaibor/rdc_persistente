{
  "name": "rdc-persistente-macos",
  "version": "0.1.0",
  "description": "Supervisor persistente para Desktop Commander Remote en macOS usando Node.js y launchd.",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/*.js && node --check bin/*.js && node --check scripts/*.js",
    "stage": "node scripts/install.js",
    "install:local": "node scripts/install.js --activate",
    "status": "node scripts/status.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AndresGaibor/rdc_persistente.git"
  },
  "keywords": ["desktop-commander", "rdc", "macos", "launchd", "watchdog"],
  "license": "MIT"
}
